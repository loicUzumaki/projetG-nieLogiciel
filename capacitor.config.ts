import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.licence3.groupe7',
  appName: 'UberEA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
